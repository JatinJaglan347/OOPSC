#include <stdio.h>
struct student{
        int roll;
        float score ;
    }
    
int main(){
    Student student [10];
    for (int i=0 ; i<10 ; i++){
        scanf ("&s",&students [i].name);
        scanf ("&d",&students [i].marks);
    }
    
    return 0;
}